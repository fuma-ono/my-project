# AI_USAGE_AUDIT.md — Claude Code利用状況・上限消費リスク監査(2026-09-15)

オーナー指示「Claude Code上限消費最適化・常時稼働監査」に基づく現状監査。**この監査ではコード変更・Routine変更を一切行っていない。読み取り専用調査のみ。**

## 0. 調査方法

- リポジトリ全体(`.github/workflows/`、`scripts`、`app`、各事業ディレクトリ、`docs`、`package.json`、`requirements.txt`、`.env.example`)を`anthropic`/`claude`/`Agent`/AI関連SDK等のキーワードで横断検索
- `list_triggers`でこのアカウントが保有する全Routine(スケジュール実行)を列挙し、各Routineの`cron_expression`・`prompt`本文・`persist_session`・`last_run`を確認
- `get_session`で、Routineが紐付く2つの永続セッションの`context_usage`・`usage`(トークン数・コスト)を確認
- `actions_list`でGitHub Actionsの実行履歴(scheduleイベント)をサンプル確認

## 1. 現状: AIコード直接呼び出しはゼロ、全AI利用はClaude Code Remote経由

リポジトリ内を`anthropic|claude[-_]?api|ANTHROPIC_API_KEY|messages\.create|@anthropic-ai`で全文検索した結果、**Pythonスクリプト・JS/TSコード・`requirements.txt`・`package.json`のいずれにもAnthropic SDK/APIへの直接呼び出しは1件も存在しない**。

つまり:
- **Claude API単体の直接利用は現状ゼロ**(セクション14「Claude CodeとClaude APIを分離」以前に、そもそもClaude API単体利用の実装自体がまだ存在しない)
- **すべてのAI処理は「Claude Code Remoteの定期Routine」という1種類の経路のみで行われている**。LEVEL 0(AI不要)からLEVEL 2(高度な戦略判断)まで、あらゆる粒度の処理が同じ「フルセッションを起動する」コストで実行されている

これが、後述する「上限消費の原因」の根本である。

## 2. Routine(スケジュール実行)一覧 — 16件

このアカウントが保有するRoutineは16件、すべて`persist_session: true`(=毎回使い捨てのセッションではなく、特定の永続セッションを再開する方式)。2つの永続セッションに分かれている。

| Routine名 | cron | 頻度(概算) | 紐付く永続セッション | Claude(判断)必要か | 分類 |
|---|---|---|---|---|---|
| Threadsトークン確認 確実に起動 | `17 */4 * * *` | 6回/日 | セッションA(アフィリエイト) | 不要 | LEVEL 0 |
| Threads Insights取得 確実に起動 | `10 21 * * 5` | 1回/週 | セッションA | 不要 | LEVEL 0 |
| Threads商品リサーチ 確実に起動 | `10 21 * * 1` | 1回/週 | セッションA | 不要 | LEVEL 0 |
| Threads返信送信 確実に起動 | `52 */2 * * *` | 12回/日 | セッションA | 不要 | LEVEL 0 |
| Threads返信取得 確実に起動 | `12 */2 * * *` | 12回/日 | セッションA | 不要 | LEVEL 0 |
| Threadsトークン 監視 | `30 */4 * * *` | 6回/日 | セッションA | 不要(単純なJSON条件分岐+通知) | LEVEL 0 |
| Threadsアフィリエイト 投稿を確実に起動 | `5 12 * * *` | 1回/日 | セッションA | 不要 | LEVEL 0 |
| Threads返信 自動対応(返信可否の判断) | `30 */2 * * *` | 12回/日 | セッションA | **必要**(内容の可否判断・文面生成) | LEVEL 2 |
| Threadsトークン延長リマインド | `0 0 1 * *` | 1回/月 | セッションA | 不要(固定文面の通知) | LEVEL 0 |
| Threadsアフィリエイト 毎日投稿案生成 | `0 11 * * *` | 1回/日 | セッションA | **必要**(コンテンツ生成) | LEVEL 2 |
| 恋愛ジャンルnote 週次記事生成 | `0 3 * * 2` | 1回/週 | セッションB(売上向上戦略) | **必要**(記事執筆) | LEVEL 1〜2 |
| モヤスカ 台本自動生成 | `0 0 * * 3` | 1回/週 | セッションB | **必要**(創作+構文検証) | LEVEL 2 |
| note記事 週2回自動下書き | `0 0 * * 1,4` | 2回/週 | セッションB | **必要**(記事執筆+品質セルフチェック) | LEVEL 1〜2 |
| BGM Shorts 毎日自動公開 | `0 12 * * *` | 1回/日 | セッションB | 大部分不要(スクリプト実行+成否判定+定型通知) | LEVEL 0〜1 |
| BGM長尺動画 週次自動公開 | `0 0 * * 2` | 1回/週 | セッションB | 大部分不要(同上) | LEVEL 0〜1 |
| 週次経営レビュー | `0 0 * * 1` | 1回/週 | セッションB | **必要**(戦略判断・複数事業の横断確認) | LEVEL 2 |

**1日あたりの起動回数(概算)**:
- セッションA: 6+1/7+1/7+12+12+6+1+12+1/30+1 ≈ **約51回/日**
- セッションB: 1+1/7+1/7+2/7+1+1/7+1/7 ≈ **約2回/日**
- **合計 約53回/日、セッションAだけで年換算1万8,000回超**

このうち、**セッションAの「確実に起動」系6件+トークン監視1件(計37回/日、全体の70%以上)はAIの判断を一切必要としない機械的処理**(GitHub Actionsのworkflow_dispatch起動→結果ポーリング→失敗時のみ報告、またはJSON条件分岐+通知)である。

## 3. 上限消費の実測データ(最重要)

`get_session`で2つの永続セッションの累積使用量を確認した(2026-08-01作成〜2026-09-15時点、約6週間分)。

### セッションA「アフィリエイト」(session_01AxR9HADDnxcJafvDr9uN9i)

| 項目 | 値 |
|---|---|
| 作成日 | 2026-08-01 |
| 現在のコンテキスト使用量 | 181,835 / 1,000,000 トークン(約18%) |
| **累積cache_read_tokens** | **811,502,867(約8.1億トークン)** |
| 累積cache_write_tokens | 25,699,133 |
| 累積input_tokens | 353,790 |
| 累積output_tokens | 1,138,812 |
| **累積コスト** | **約$229.79** |

### セッションB「売上向上戦略:動画・アプリ開発」(session_01Vf7j1fdqmbJAdthZKd5PVw)

| 項目 | 値 |
|---|---|
| 作成日 | 2026-08-01 |
| 現在のコンテキスト使用量 | 576,050 / 1,000,000 トークン(約58%) |
| 累積cache_read_tokens | 218,356,698(約2.2億トークン) |
| 累積cache_write_tokens | 6,940,447 |
| 累積input_tokens | 994,699 |
| 累積output_tokens | 262,086 |
| 累積コスト | 約$51.71 |

**2セッション合計で約$281の累積コスト、cache_read_tokensだけで約10億トークン**(約6週間で)。

### この数字が意味すること

- `persist_session: true`のRoutineは**同じ会話履歴を毎回再読み込みして再開する**。会話は一度も要約・リセットされていないため、Routineが起動するたびに「これまでの全会話」がキャッシュから読み込まれる(cache_read_tokens)。
- セッションAは1日約51回起動し、起動のたびに蓄積済みの会話全体を読み込むため、cache_read_tokensが桁違いに膨らんでいる(セッションBの約3.7倍)。これは**起動回数(頻度)がそのままコストに直結している**ことを示す直接的な証拠である。
- セッションAのコンテキスト使用量自体はまだ1,000,000トークンの18%だが、**このペースで増え続ければ将来的にコンテキスト圧迫(要約・古い情報の切り捨て)も発生し得る**(セクション8「長時間セッション化の防止」で懸念されている通り)。セッションBは既に58%に達しており、より近い将来にこの問題へ到達する。

## 4. 危険な自動化(重点確認項目への回答)

### 4-1. 「毎日」「毎時」「数分ごと」でClaudeを起動している処理

上記の表の通り、**セッションAだけで1日51回**(2時間おき×3種、4時間おき×2種を含む)。これは会社全体のAI起動回数の96%を占める。

### 4-2. 無限ループ・過剰実行リスク

- `threads-affiliate/automation_guard.py`(サーキットブレーカー)が**Threadsの投稿・返信の連続失敗**は検知・自動停止する仕組みを持つ(`MAX_CONSECUTIVE_FAILURES = 3`)。これは既に実装済みで機能している。
- ただし、この仕組みは**Threads自動化(投稿・返信)専用**であり、以下はカバーされていない:
  - BGM/moyasuka公開の失敗ループ(現状は都度PushNotificationで報告するのみ、自動停止機構なし)
  - **AI(Claude Code)自体の起動回数・コストに対する上限・自動停止の仕組みはリポジトリ全体でゼロ**(セクション15で後述)
- 「確実に起動」系Routineはpolling(`actions_list`で完了まで数回ポーリング)を伴うが、各Routineに具体的なポーリング回数上限は明記されておらず、GitHub Actions側が長時間`in_progress`のままになった場合の挙動は未定義(実害は今のところ確認されていないが、明文化されたリトライ上限がない点はリスク)。

### 4-3. コンテキスト過剰利用箇所

- セッションA・Bともに、Routineプロンプト内で明示的な事前情報の絞り込みは行われていない。各Routineは起動のたびに`git pull`や複数ファイルの読み込みを行う設計であり、蓄積された会話履歴の上にさらに読み込みが積み重なる。
- 特に「週次経営レビュー」「BGM長尺動画週次自動公開」「モヤスカ台本自動生成」は、複数事業を横断する長い指示文(1,500〜2,600文字)を持ち、実行のたびに複数ファイル(`docs/dashboard/template.html`、各事業の設定ファイル等)を読み書きするため、1回あたりの処理量も他のRoutineより重い。

## 5. AIが不要な処理(LEVEL 0への降格候補)

| 処理 | 現状 | 判定理由 |
|---|---|---|
| Threadsトークン確認 確実に起動 | Claude Code Remote(4時間おき) | `workflow_dispatch`実行→ポーリング→失敗時報告、のみ。判断要素なし |
| Threads Insights取得 確実に起動 | 同上(週次) | 同上 |
| Threads商品リサーチ 確実に起動 | 同上(週次) | 同上 |
| Threads返信送信 確実に起動 | 同上(2時間おき) | 同上 |
| Threads返信取得 確実に起動 | 同上(2時間おき) | 同上 |
| Threadsアフィリエイト 投稿を確実に起動 | 同上(毎日) | 同上 |
| Threadsトークン 監視 | Claude Code Remote(4時間おき) | `token-health.json`の`"ok"`フィールドを読むだけの条件分岐+通知。if文で完結する |
| Threadsトークン延長リマインド | Claude Code Remote(月次) | 固定文面をオーナーに伝えるだけ |
| BGM Shorts/長尺 自動公開の**成否判定・通知部分** | Claude Code Remote | スクリプトの終了コード確認・OAuth残日数の数値比較・定型メッセージ送信は条件分岐で完結する(ただし「毎日稼働への切替が視聴者離れを起こしていないか」等の判断部分はLEVEL 1〜2として残す余地あり) |

**上記だけで、セッションAの1日51回のうち約37回(72%)がAI不要のLEVEL 0処理**である。

## 6. AIを残すべき処理

| 処理 | 理由 |
|---|---|
| Threads返信 自動対応 | ガイドライン(`docs/marketing/2026-09-10-threads-auto-reply-guardrails.md`)に基づく可否判断・文面生成は文脈理解を要する。ただし2時間おきという頻度は要検討(セクション「推奨変更」参照) |
| Threadsアフィリエイト 毎日投稿案生成 | 商品選定・フック作成・文章構成はLLMの価値が高い創作領域 |
| note記事生成(2種)・モヤスカ台本生成 | 同上、創作領域 |
| 週次経営レビュー | 複数事業を横断した戦略判断・優先順位付けはLLMでなければ困難 |
| BGM週次のコンテンツ戦略判断(プリセット追加等) | 実績データに基づく方針転換の判断はLLMの価値がある領域 |

## 7. GitHub Actions側の監査

| Workflow | 頻度 | Trigger | Claude Code使用 | 実行目的 | 備考 |
|---|---|---|---|---|---|
| kashikari-ios-manual-build.yml | 手動のみ | workflow_dispatch | No | iOSビルド | AI不使用、問題なし |
| note-publish-check.yml | 6時間おき | schedule | No | note.com RSS監視・自動フラグ更新 | 純コード、模範的 |
| threads-fetch-replies.yml | 2時間おき | schedule | No(GitHub Actions側はPythonのみ) | コメント取得 | ワークフロー自体はAI不使用。ただし**同じ処理を確実化するためClaude Code Remote Routineが並走**(セクション2) |
| threads-insights.yml | 週次 | schedule | No | 実績取得 | 同上 |
| threads-publish.yml | 毎日+バックアップ2回 | schedule | No | 投稿実行 | 同上。**GitHub Actions側の`schedule`実行履歴を確認したところ、直近の実行時刻は設定値(21:05/21:35/22:05 JST)からずれている(例: 実際の起動が17:52〜18:32 UTC台に集中)ものの、概ね毎日実行はできている** |
| threads-research.yml | 週次 | schedule | No | 商品リサーチ | 同上 |
| threads-send-replies.yml | 2時間おき | schedule | No | 返信投稿 | 同上 |
| threads-token-health-check.yml | 4時間おき | schedule | No | トークン疎通確認 | 同上 |

**GitHub Actions自体は8本すべてAI不使用、模範的な設計**。問題は「これらのGitHub Actionsのscheduleトリガーが信頼できないため、Claude Code Remoteの永続セッションを"確実に起動させるための保険"として使っている」という構造そのものにある。

`threads-fetch-replies.yml`のGitHub Actions実行履歴(`event: schedule`のみ)を確認したところ、2026-09-11以降の約4日間で**19回の完了ログ**が確認できた(cron設定は2時間おき=1日12回、4日で48回が期待値)。native cronの成功率は50%を下回っている可能性が高く、Claude Code Remote側でのバックアップ起動が実際に必要とされてきた背景は理解できる。**ただし、その解決策としてフルのClaude Codeセッションを使うのはコストが不釣り合いに大きい**(セクション9「推奨変更」で代替案を提示)。

## 8. AI利用量の可視化(現状)

- Claude Code Remote側の`get_session`/`list_triggers`で累積コスト・トークン数は取得できる(本監査で使用した通り)が、**リポジトリ側にはこれらを日次で記録・可視化する仕組みが存在しない**。
- `docs/company-os/kpi/`にはBGM・noteの再生数KPIはあるが、AI実行回数・トークン消費・コストの記録は無い。
- 安全装置(利用量が異常に増えた場合の自動停止)も、Threadsの投稿・返信失敗に限っては存在するが(`automation_guard.py`)、**AI起動回数そのものに対する上限は存在しない**。

## 9. まとめ(数字での結論)

- Claude Code Remoteの永続セッションは2つ、**Routine 16件すべてがこの2セッションのどちらかを再利用**しており、会話は一度もリセットされていない。
- **1日約53回のRoutine起動のうち、約37回(70%)はAIの判断を必要としない機械的処理**(GitHub Actions起動確認・JSON条件分岐+通知)。
- この機械的処理の大部分は、**セッションAを1日37回無駄に「起こして会話履歴を読み込ませている」**ことに相当し、実測でcache_read_tokensが約6週間で8.1億トークン・コスト約$230に達している主因と考えられる(セッションBは起動頻度が少なく、コストも約1/4)。
- リポジトリのコード自体にはAnthropic API直接呼び出しは無く、Claude Code Remoteへの依存が100%。

次のステップは`AI_AUTOMATION_ARCHITECTURE.md`(推奨アーキテクチャ)と`AI_USAGE_POLICY.md`(今後の運用ルール)を参照。**いずれも提案段階であり、Routine・ワークフローの変更はオーナー確認後に別途実施する。**
