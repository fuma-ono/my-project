# AI Usage Audit — Claude Code消費監査

**実施日**: 2026-09-15
**実施者**: 社長(Claude, session_01Vf7j1fdqmbJAdthZKd5PVw)
**スコープ**: リポジトリ全体のコード監査 + Claude Code Remoteのセッション/Routine構成監査
**方針**: 今回はコード変更を行わない。監査と報告のみ。

---

## 0. 結論(先に要点)

**上限消費の最大の原因は、リポジトリのコードでもGitHub Actionsでもない。「Claude Code Remoteの常時稼働セッションが複数、並行して"WORKING"状態のまま存在している」ことである。**

| # | セッション | 状態 | 推定コスト | 原因 |
|---|---|---|---|---|
| 1 | **アプリ開発**(kashikari) | **WORKING(稼働中)** | **$1,011.34** | Routineに紐付かない長時間セッション。原因不明、直接調査が必要 |
| 2 | **アフィリエイト**(threads-affiliate/love-note) | **WORKING(稼働中)** | **$229.79** | 10個のRoutineが2〜4時間おきに発火し続けている |
| 3 | **売上向上戦略**(本セッション) | WORKING | $51.71 | 6個のRoutine(日次〜週次)+今回の監査作業 |
| 4 | **GROWTH-HQ** | **WORKING(稼働中)** | $42.18 | 作成から4日でこの額。Routine紐付けなし、原因不明 |
| 5 | 売上向上戦略(入力不可) | BLOCKED/IDLE | $5.05 | コンテナ起動失敗で停止中の孤立セッション |
| | **合計** | | **約$1,340** | |

GitHub Actionsは2本しかなく、どちらもClaude/Anthropicを一切呼び出していない(後述)。リポジトリの自動化コード(bgm-pipeline / moyasuka / note-articles / x-account)にも、Anthropic SDKへの依存は一切ない。**つまり「気づかないうちに消費している」のは、コードの外側にある"Claude Code Remoteの放置されたセッション"そのものである。**

---

## 1. GitHub Actions監査(A)

リポジトリ内のワークフローは **2本のみ**。

| Workflow名 | 実行頻度 | Trigger | Claude使用 | Claude Code使用 | Claude API使用 | 実行目的 | AIが本当に必要か | 通常コードへ置換可能か | 推定コスト/消費 | 改善案 |
|---|---|---|---|---|---|---|---|---|---|---|
| `note-publish-check.yml` | 6時間毎 | schedule + workflow_dispatch | **No** | **No** | **No** | note.com RSSをポーリングし、公開検知した記事のステータスを自動フリップ | 不要(既に通常コード) | ✅ 既に通常コードで実装済み | GitHub Actions無料枠のみ、$0 | **これが理想形。他のRoutineもこのパターンに寄せるべき** |
| `kashikari-ios-manual-build.yml` | 手動のみ(`workflow_dispatch`) | 手動 | **No** | **No** | **No** | iOSアプリの手動ビルド(EAS無料枠の代替) | 不要 | ✅ 既に通常コードで実装済み | macOSランナー消費のみ(手動実行時) | 問題なし |

**結論**: GitHub Actionsは全く無駄がない。むしろ`note-publish-check.yml`は「AIが不要な定型処理をコード化する」という今回の目的そのものを先取りして実装済みの好例。**この方式を横展開すべき。**

---

## 2. リポジトリコード内のAI呼び出し監査

`anthropic` / `Claude Code` / `ANTHROPIC_API_KEY` 等のキーワードでリポジトリ全体を検索した結果:

- **Anthropic SDKへの直接依存は0件**(全`requirements.txt`・`package.json`を確認)
- ヒットした13ファイルは全て「Claude Codeセッションからは特定の外部サービスに到達できない」という**制約についての説明コメント**であり、AI呼び出しのコードではない(例: `note_publish/note_auth.py`のコメント「Claude Code cloud sessionはnote.comへ到達できない」)
- `app/.claude/settings.json` はExpo開発用のClaude Codeプラグイン設定であり、実行時のAI呼び出しとは無関係

**結論**: 事業側の自動化コード(bgm-pipeline / moyasuka / note-articles / x-account)は、すべて「純粋なプログラム」として実装されている。AIが介在するのは、**Claude Code Remoteのセッション自体がRoutineに起動されてBashコマンドを実行する**、という1経路のみ。

---

## 3. Claude Code Remote Routine監査(本アカウント全体、16件)

`list_triggers`で本アカウントが保有する全Routineを取得した結果、**16件の定期実行Routine**が存在し、**2つの異なるセッションに分散**して紐付いている。

### 3-A. 本セッション(session_01Vf7j1...)に紐付く6件

| Routine名 | 頻度 | 実行目的 | AIが本当に必要か | 通常コードへ置換可能か |
|---|---|---|---|---|
| BGM Shorts 毎日自動公開 | 毎日 | 固定コマンド`publish_shorts --seconds 45 --privacy public`の実行、成功/失敗判定 | **成功経路は不要**(判断が要るのは失敗時のOAuth再認証依頼のみ) | ✅ ほぼ全面的に可能(後述LEVEL 0) |
| BGM長尺動画 週次自動公開 | 週1 | 同上、`--minutes 60` | 同上 | ✅ ほぼ全面的に可能 |
| モヤスカ 台本自動生成 | 週1 | 在庫本数のカウント→閾値未満なら生成 | カウント・閾値判定は不要。台本の文章生成のみAI価値あり | 🔶 一部可能(カウント処理は完全にLEVEL 0) |
| note記事 週2回自動下書き | 週2 | 同上(在庫管理)+ テーマ選定 + 記事本文生成 | 在庫管理は不要。記事本文生成・品質チェックはAI価値あり | 🔶 一部可能(在庫管理・キュー同期はLEVEL 0) |
| 週次経営レビュー | 週1 | OAuth横断チェック、ダッシュボード更新、週次報告書生成、コンテンツ戦略判断 | OAuth日数チェックは不要。戦略判断・分析文章はAI価値あり | 🔶 一部可能(OAuth確認・カウント系はLEVEL 0) |

**直近16日間の実態**: `client_secret.json`消失によりBGM関連の投稿ができない状態が続いており、**BGM Shorts(毎日)・BGM長尺(週1)は、この16日間毎回「認証情報の有無を`ls`で確認して即終了」という1コマンドのためだけにClaude Codeセッションを起動し続けている。** これは本監査が指摘する「AIが不要な処理でAIを起動している」の典型例そのもの。

### 3-B. 別セッション「アフィリエイト」(session_01AxR9...)に紐付く10件 ★最大の消費源

| Routine名 | 頻度 | 1日あたり発火回数 |
|---|---|---|
| Threads返信送信 確実に起動 | 2時間毎 | 12回 |
| Threads返信取得 確実に起動 | 2時間毎 | 12回 |
| Threads返信 自動対応 | 2時間毎 | 12回 |
| Threadsトークン確認 確実に起動 | 4時間毎 | 6回 |
| Threadsトークン 監視 | 4時間毎 | 6回 |
| Threadsアフィリエイト 毎日投稿案生成 | 毎日 | 1回 |
| Threadsアフィリエイト 投稿を確実に起動 | 毎日 | 1回 |
| Threads商品リサーチ 確実に起動 | 週1(月) | 0.14回 |
| Threads Insights取得 確実に起動 | 週1(金) | 0.14回 |
| Threadsトークン延長リマインド | 月1 | 0.03回 |
| 恋愛ジャンルnote 週次記事生成(love-note) | 週1(火) | 0.14回 |

**小計: 1日あたり約50回のセッション起動**(本セッション分の約2.3回/日と比べて**約22倍**)。

**特に問題な点**:
1. **「返信送信」「返信取得」「返信 自動対応」の3本が、いずれも2時間おきに独立して発火している。** 名前から見て役割が重複・分割されており、本来1本のRoutine(取得→判断→送信を1回のセッションで完結)にできる可能性が高い。3本に分かれている場合、1往復の返信処理のために最大3回のセッション起動が発生し得る。
2. **「確実に起動」という命名が6本に付いている。** これは過去に「起動しない」問題が起きたためのバックアップ/保険的Routineだと推測されるが、**オリジナルのRoutineと二重に発火していないか要確認**(名前の付け方から、元のRoutineが別に存在し、それが不安定だったために予備を追加した可能性がある)。
3. **このセッションは`claude/note-issue-1pjubh`ブランチで動作している。** このブランチは本セッションの複数のRoutineプロンプトで「**絶対にコミットしない**」「**回収すべき内容はない**」と明記されている、2026-08-10に判明した"古い放棄済み分岐"である。**放棄されたはずのブランチ上で、今なお1日50回のペースでAIセッションが起動され続けている。**

### 3-C. その他の稼働中セッション(Routineに紐付かない)

| セッション名 | 作成日 | 状態 | 推定コスト | 備考 |
|---|---|---|---|---|
| **アプリ開発**(kashikari) | 2026-08-20 | **WORKING** | **$1,011.34** | 16個のRoutineリストのいずれにも紐付いていない。にもかかわらず本アカウント全体で**最大の消費源**。長時間の対話的セッション、または何らかのループが継続している可能性が高く、直接開いて内容を確認する必要がある |
| **GROWTH-HQ** | 2026-09-11(4日前) | **WORKING** | $42.18 | 同じくRoutine紐付けなし。4日間で$42は、放置すればさらに増加するペース |
| 売上向上戦略(入力不可) | 2026-09-01 | BLOCKED | $5.05 | コンテナ起動が20分スタックし、ユーザー操作待ちのまま放置。実害は小さいが「気づかれずに残っているセッション」の実例 |

---

## 4. AIが不要な処理(LEVEL 0)の特定

| 処理 | 現状 | 判定理由 |
|---|---|---|
| OAuth残り日数の確認・分岐(`youtube_auth.days_until_refresh_expiry()`の値を見て通知するかどうか決める) | AIが毎回判定 | 単純な数値比較(`< 2` / `< 3`)。通常コードのif文で完結する |
| `credentials/`ディレクトリの存在確認 | AIが`ls`コマンドで確認 | ファイル存在チェックのみ |
| BGM Shorts/長尺の「成功時」の処理(固定コマンド実行→git commit→push) | AIがBashコマンドを発行 | 分岐なしの固定シーケンス。スクリプト化可能 |
| note記事・モヤスカ台本の在庫本数カウント、閾値判定 | AIがJSONを読んでカウント | `jq`や5行のPythonで完結する |
| note投稿キューページのHTML再生成・Artifact再公開 | AIがbuild.py実行→Artifact公開 | build.pyは既に通常コード。Artifact公開APIさえ叩ければ完全無人化可能 |
| RSS自動検知による投稿ステータス更新 | **既に通常コード化済み**(`note-publish-check.yml`) | 理想形。他の「投稿確認」系Routine(Threads Insights取得等)も同じパターンに移行できるはず |
| Threadsトークンの有効期限確認・監視 | AIが2種類のRoutineで確認 | YouTube OAuthと同じ、単純な期限比較 |

## 5. AIを残すべき処理(LEVEL 1〜2)

| 処理 | レベル | 理由 |
|---|---|---|
| note記事の本文生成(テーマ選定・執筆・品質セルフチェック) | LEVEL 1〜2 | 「機械翻訳調でないか」「読者価値があるか」等の定性判断が必要 |
| モヤスカ台本の執筆 | LEVEL 1 | 定型フォーマットに沿うが、セリフ内容の創作が必要 |
| BGMコンテンツ戦略判断(新バリエーション追加の要否) | LEVEL 2 | 実績データからの複合的な判断、人間承認に近い重み |
| 週次経営レビューの分析・報告書ナラティブ | LEVEL 2 | 複数事業を横断した状況判断・優先順位付け |
| 障害の根本原因調査(今回のcredential消失事故対応など) | LEVEL 2 | 原因不明の問題の調査・切り分け |
| Threads返信の内容生成(何を返信するか) | LEVEL 1 | 文面生成自体は軽量AIで足りる可能性が高い(戦略判断ではない) |

## 6. 無限ループ・過剰実行リスク

- **直接的な無限ループは確認されなかった**(各Routineは固定cronで、失敗時に即座に再起動する設計ではない)
- ただし、**「確実に起動」という名前の6 Routineが、元のRoutineと重複して同じ処理を行っていないか**は、このリポジトリの情報だけでは確認しきれない。二重発火であれば、実質的な消費が現在の数字よりさらに大きい可能性がある
- **最大リトライ回数・クールダウン・同一エラー検知による強制停止の仕組みは、いずれのRoutineにも実装されていない**(存在するのは「事前チェックで問題があれば今回は何もせず終了する」という一方向のガードのみ)
- **「WORKING」状態のまま長時間残り続けるセッション**(kashikari、GROWTH-HQ)は、無限ループそのものではないが、**「安全装置なしに稼働し続けられる」という設計上の穴**を体現している

## 7. コンテキスト過剰利用箇所

- 各Routineのプロンプトは数百〜千文字程度で、それ自体は軽量
- しかし**セッションが「持続セッション(persistent_session_id)」に紐付いているため、Routineが発火するたびに、そのセッションが積み上げてきた全会話履歴(本セッションの場合、既に数週間・数百ターン分)を読み込んだ状態で再開する**。これが`context_usage`(本セッション: 576,050 / 1,000,000トークン、アフィリエイトセッション: 181,835 / 1,000,000トークン、kashikariセッション: 282,128 / 1,000,000トークン)に表れている
- 「OAuth未認証だったのでスキップしました」という1行の結論のために、数十万トークンの会話履歴付きセッションが毎回呼び出されている状態

## 8. 改善によって削減できそうなAI実行量(試算)

| 施策 | 削減対象 | 効果 |
|---|---|---|
| BGM Shorts/長尺の「認証チェック→スキップ」をGitHub Actions化 | 本セッション6 Routineのうち2つの大部分 | 認証復旧まで続く限り、毎日1回・週1回の無駄な起動をゼロに |
| Threads返信3系統の統合・重複排除 | アフィリエイトセッション | 最大で1日36回→1日12回程度まで削減できる可能性(重複がある場合) |
| 「確実に起動」系Routineの重複確認・整理 | アフィリエイトセッション6件 | 重複が確認できれば即座に半減 |
| 在庫カウント・閾値判定・キュー同期のコード化 | 全事業のRoutine | セッション起動自体は残るが、コンテキスト消費・思考時間を大幅削減 |
| kashikari(アプリ開発)・GROWTH-HQセッションの内容確認と要否判断 | 最大消費元($1,011+$42) | **本アカウント全体のコスト構造上、最優先で確認すべき項目** |
| 放置セッションのアーカイブ(BLOCKED状態のもの) | 売上向上戦略(入力不可) | 実害は小さいが、可視性向上のため整理を推奨 |

---

## 9. まとめ表(オーナー報告用)

| 項目 | 内容 |
|---|---|
| GitHub Actionsの問題 | **なし**。既に理想形で実装されている |
| リポジトリコードの問題 | **なし**。Anthropic SDK依存ゼロ、全て通常コード |
| 最大の問題 | **Claude Code Remoteの複数セッションが並行して常時稼働状態**($1,340相当)。特に紐付くRoutineが見当たらない「アプリ開発」セッションの$1,011が最大の謎 |
| 二番目の問題 | 「アフィリエイト」セッションの10 Routineが2〜4時間おきに発火(1日約50回) |
| 三番目の問題 | 本セッションの6 Routineのうち、認証待ちで16日間「チェックしてスキップ」を繰り返している分 |
| 危険な自動化 | 放棄済みブランチ(`claude/note-issue-1pjubh`)上で今も高頻度自動化が動き続けている |
