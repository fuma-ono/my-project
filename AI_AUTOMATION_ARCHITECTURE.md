# AI Automation Architecture — 改善後アーキテクチャ案

**作成日**: 2026-09-15
**前提**: `AI_USAGE_AUDIT.md`の監査結果に基づく設計案。**この文書自体はまだ実装しない。** オーナー確認後、優先順位に沿って段階的に適用する。

---

## 1. 目標アーキテクチャ(全体像)

```
                    ┌──────────────────┐
                    │  GitHub Actions   │  ← 定型処理・データ取得・監視
                    │  (cron / dispatch)│     GitHub無料枠、$0
                    └─────────┬─────────┘
                              │
                    定期実行・状態確認・条件判定
                              │
                              ▼
                    ┌──────────────────┐
                    │  Database / JSON  │  ← rotation_state.json,
                    │  (既存の軽量ファイル) │     generation-log.json 等の延長
                    └─────────┬─────────┘
                              │
                       異常 or 判断必要?
                        /            \
                      No              Yes
                      │                │
                      ▼                ▼
                   終了(AI起動なし)   Claude API 呼び出し
                                        (Claude Code Remoteではない)
                                        │
                                        ▼
                                  AI判断・文章生成
                                        │
                                        ▼
                                  アクション実行・コミット
                                        │
                                        ▼
                                     ログ保存

Claude Code Remote(本セッションのような持続セッション)は、
この常時稼働経路から外す。位置付けは:

  Claude Code Remote
      ↓
  人間が明示的に呼ぶ/確認する場面のみ
      ↓
  開発・障害調査・戦略判断・週次レビューのとりまとめ
```

**核心の変更点**: 現在は「Routine → Claude Code Remoteセッションを毎回フル起動 → Bashコマンド実行」という1本道になっている。これを「GitHub Actions(通常コード)が大半を処理し、判断が必要な場面だけAIを呼ぶ」という**二層構造**に分離する。

---

## 2. Routineの再分類(現状16件 → 改善後の姿)

### 2-A. GitHub Actions化する(LEVEL 0、Claude Code Remoteから完全に外す)

| 現在のRoutine | 移行先 | 実装方針 |
|---|---|---|
| BGM Shorts 毎日自動公開(の認証チェック部分) | GitHub Actions(`cron: "0 12 * * *"`) | `youtube_auth.days_until_refresh_expiry()`相当をPythonスクリプト化。2日未満/未認証ならissueコメント or Slack/メール通知のみ発行し、AIは呼ばない。認証OKなら`publish_shorts`を直接実行(既存の`publish_shorts.py`はそのまま流用可能、AIが介在する必要は元々ない) |
| BGM長尺動画 週次自動公開(同上) | GitHub Actions(`cron: "0 0 * * 2"`) | 同上パターン |
| モヤスカ台本の在庫カウント・閾値判定 | GitHub Actions or シンプルなCLI | `ls moyasuka/scripts/ | wc -l`相当。閾値以上ならAI不要、そのまま終了 |
| note記事の在庫カウント・閾値判定 | GitHub Actions | `generation-log.json`をjqで集計するだけ |
| note投稿キューページのbuild・Artifact再公開 | GitHub Actions + Artifact API | `build.py`は既に通常コード。Artifact公開APIをスクリプトから叩ければ完結 |
| Threadsトークンの有効期限確認・監視(2 Routine) | GitHub Actions(`cron: 0 */4 * * *`) | YouTube OAuthと同じ、期限比較のみ |
| Threads Insights取得 | GitHub Actions(週次) | API呼び出し+データ保存のみ、AI不要 |
| RSS自動検知(note publish check) | **既に完了**(`note-publish-check.yml`) | 変更不要、他の移行のリファレンス実装として使う |

### 2-B. Claude APIに移行する(LEVEL 1、軽量・都度課金・持続セッションなし)

| 現在の処理 | 移行方針 |
|---|---|
| Threads返信の文面生成 | GitHub Actionsが「返信が必要なメンションがある」ことを検知した"ときだけ"、Claude APIを1回呼んで文面を生成→即座に投稿→終了。持続セッションを維持しない |
| モヤスカ台本の本文生成 | 在庫が閾値未満と分かった時点で、GitHub ActionsからClaude APIを呼び出し、台本テキストを生成してコミット。人間承認が要らない範囲(既存フォーマットの遵守)なので、Claude Code Remoteの持続セッションは不要 |
| note記事本文の生成・品質チェック | 同上。ただし「既存記事との重複回避」判定にはある程度のコンテキスト(既存記事一覧)が要るため、必要なデータのみJSON化してAPIに渡す(9章参照) |

### 2-C. Claude Code Remoteに残す(LEVEL 2、判断・戦略・開発)

| 処理 | 理由 |
|---|---|
| BGMコンテンツ戦略判断(新バリエーション追加の要否) | 複数指標を横断した複合判断、人間承認に近い重みを持つ |
| 週次経営レビューのとりまとめ・分析ナラティブ | 4事業横断の状況判断、優先順位付け |
| 障害調査(今回のcredential消失のような) | 原因不明の問題の切り分けには開発者としての判断が要る |
| リポジトリの大規模改修・新機能開発 | 元々の用途そのもの |
| 今回のようなアーキテクチャ監査・改善提案 | 同上 |

**この結果、Claude Code Remoteの持続セッションが定期的に(cronで)起動される場面は、実質「週次経営レビュー」1本にまで減らせる可能性が高い。** それ以外は「GitHub Actionsが異常/必要性を検知した場合にだけ」起動する、Event Driven方式に切り替える。

---

## 3. Event Driven方式への転換(6章の指示に対応)

現状(Poll方式):
```
Claude Codeセッション
  ↓ (毎日/毎週、無条件に起動)
状態確認(OAuth日数、在庫本数など)
  ↓
問題なし → 何もせず終了(それでもセッション起動分のコストは発生済み)
```

改善後(Event Driven方式):
```
GitHub Actions(軽量・無料)
  ↓ (同じ頻度で起動して良い。コストがほぼゼロのため)
状態確認(通常コード)
  ↓
問題なし → 終了(AI起動なし、コストゼロ)
  ↓
異常あり or 生成が必要 → Claude API(単発)を呼ぶ、または
                         Issue/通知を発行してオーナーに知らせる
  ↓
それでも人間の戦略判断が必要 → Claude Code Remoteセッションへ
                                  明示的にプロンプトを送る
                                  (現状の`send_later`/`fire_trigger`と
                                   同じ仕組みを使い回せる)
```

**重要**: 「頻繁にチェックすること」自体は問題ではない。問題は「チェックのたびにフル装備のAIセッションを起動すること」。チェック自体はGitHub Actionsに任せて頻度を維持し、AIを呼ぶのは「本当に判断が必要になった瞬間」だけにする。

---

## 4. Claude Code Remoteセッションの整理(最優先・監査結果への直接対応)

コード変更ではないが、アーキテクチャ上最優先で対処すべき項目:

1. **「アプリ開発」(kashikari)セッション($1,011.34、Routine紐付けなし)を直接開いて、何が起きているか確認する。** 監査時点で`status_bucket: WORKING`のまま。無限ループ、暴走したサブエージェント、あるいは単に長時間の対話が続いているだけかもしれないが、この情報だけでは判別できない。**オーナー自身がセッションを開いて確認するか、明示的な許可のもと本セッションから`interrupt_session`で一時停止し、状態を確認することを推奨する。**
2. **「GROWTH-HQ」セッション($42.18、4日で到達)も同様に確認する。** 放置すれば同じペースで伸び続ける。
3. **「アフィリエイト」セッションの10 Routineについて、「確実に起動」系6本が元のRoutineと重複していないかをオーナーに確認する。** 重複していれば、古い方を`delete_trigger`で削除するだけで最大半減が見込める。
4. **`claude/note-issue-1pjubh`ブランチ上での自動化継続の是非をオーナーに確認する。** このブランチは本セッションの複数Routineプロンプト上で明示的に「放棄済み」と記録されているにもかかわらず、そこで高頻度の自動化が今も動いている。意図的な運用なのか、単なる設定の取り違えなのかの切り分けが必要。
5. **BLOCKED状態の孤立セッション(売上向上戦略・入力不可)をアーカイブする。** 実害は小さいが、可視性を保つための整理。

---

## 5. AI利用量の可視化(13章対応)

現状、Claude Code Remoteの`list_sessions`/`get_session`から`cost_usd`・`usage`(トークン数)は取得できるが、**Routine単位・事業単位でのブレークダウンは存在しない。**

推奨する最小実装:

```json
// docs/company-os/ai-usage-log.json (新設案、実装時に検討)
{
  "entries": [
    {
      "date": "2026-09-15",
      "session_id": "session_01Vf7j1...",
      "session_title": "売上向上戦略",
      "trigger_name": "BGM Shorts 毎日自動公開",
      "outcome": "skipped_no_credentials",
      "estimated_cost_delta_usd": null
    }
  ]
}
```

- 各Routineの発火終了時に、そのRoutine自身が(可能な範囲で)1行追記する運用から始める
- `get_session`の`usage.cost_usd`はセッション累積値のため、Routine単位の差分は「前回記録時との差分」で近似する
- 将来的に、週次経営レビューでこのログを集計し、「今週どのRoutineがどれだけAIを消費したか」をダッシュボードに追記する

---

## 6. 安全装置(15章対応)

現状のRoutine実行回数(監査結果より)を踏まえた初期閾値案:

| 条件 | 閾値案 | 根拠 |
|---|---|---|
| 1時間あたりのセッション起動回数(アカウント全体) | 10回を超えたら警告、20回を超えたら該当セッションを一時停止 | 現状「アフィリエイト」セッションだけで最大6回/時間(2時間毎×3 Routine+4時間毎×2 Routineが重なる時間帯)。それを大きく超える頻度は異常とみなせる |
| 1日あたりのセッション起動回数(セッション単位) | 30回/日を超えたら警告 | 現状の「アフィリエイト」セッションが約50回/日で、既にこの水準を超えている。監査結果を受けての是正が前提 |
| 同一Routineの同一エラーでの連続失敗 | 3回連続で同一原因なら、Routineを自動`enabled: false`にしてオーナーに通知 | 現状「BGM Shorts」は16日間連続で同じ理由(client_secret.json不在)でスキップされている。**現状は実害が小さい(AIが1コマンドで即終了する設計のため)が、これがもし課金の大きい処理だった場合、16日間気づかれないまま浪費され続けていたことになる** |
| セッションの`context_usage`が閾値超過 | 800,000/1,000,000トークンを超えたら、新規セッションへの切り替えを提案 | 持続セッションのコンテキスト肥大化を防ぐ |
| 単一セッションの累積`cost_usd` | $100を超えたセッションは週次レビューで棚卸し対象にする | 「アプリ開発」「アフィリエイト」は既にこの閾値を大幅に超えている |

これらは**Claude Code Remoteのプラットフォーム機能として実装が必要な項目**が含まれており、本リポジトリのコードだけでは完結しない。オーナーへの報告事項として明記する。

---

## 7. 移行時の注意点

- **いきなり全Routineを移行しない。** まず最も安全でコード変更が少ない「BGM Shorts/長尺の認証チェック部分」から着手し、動作確認後に横展開する
- **既存のRoutineを削除する前に、必ず新しいGitHub Actionsが正常動作することを確認する**(並行稼働期間を設ける)
- **`note-publish-check.yml`は改修不要。他の移行の際のテンプレートとして再利用する**
