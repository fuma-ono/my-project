# AI_USAGE_POLICY.md — Claude Code / Claude API 利用ポリシー(2026-09-15制定)

`AI_USAGE_AUDIT.md`・`AI_AUTOMATION_ARCHITECTURE.md`の監査結果を受けて、今後の新規自動化・既存自動化の変更時に適用する原則を定める。

## 1. 基本原則

**Claude Codeを常時稼働する実行エンジンとして扱わない。**

Claude Code(このセッション/Claude Code Remote Routine)が担当してよいのは以下に限る:

- 開発・設計・大規模なコード変更
- 複雑な問題の調査・システム監査
- 創作(記事・投稿文・台本等のコンテンツ生成)
- 複数の情報を踏まえた戦略判断・優先順位付け
- 人間の承認が必要な意思決定の整理・提案

以下は原則としてClaude Code(Routine)から切り離し、GitHub Actions・cron・通常プログラムで実行する:

- 定期実行の「確実な起動」そのもの(workflow_dispatch起動・ポーリング)
- 単純なJSON条件分岐(`if ok: ... else: ...`で書けるもの)
- 定型文面の通知
- データ取得・集計・フィルタリング・重複排除・フォーマット変換
- ヘルスチェック・リトライ

## 2. 新規Routine作成時のチェックリスト

新しい定期実行(Routine)を作る前に、必ず以下を自問する。

1. **この処理は本当にLLMの判断が必要か?** if文・数値比較・正規表現で書けるなら通常コード(GitHub Actions)にする
2. **必要な頻度は?** 「念のため頻繁に」を避け、実際の業務要件から逆算する(例: SNS返信は数時間の遅延が許容範囲か? であれば2時間おきより4時間おき/1日数回で十分)
3. **判断結果はキャッシュ・保存できるか?** 同じ入力に対して毎回AIに聞き直す設計になっていないか確認する
4. **エラー時の無限ループ対策はあるか?** 最大リトライ回数・クールダウン・同一エラー検知・自動停止(`threads-affiliate/automation_guard.py`と同等の仕組み)を用意する
5. **Claude Code Remoteの永続セッション(`persist_session: true`)に紐付けるべきか?** 会話履歴を積み上げる必要がない単発処理であれば、都度使い捨てのセッション(`persist_session: false`相当)を検討する

このチェックリストを通過し、かつLEVEL 1(軽量AI)またはLEVEL 2(高度AI)に分類される処理のみ、新規Routineとして追加してよい。

## 3. LEVELの定義(再掲・運用基準)

| LEVEL | 内容 | 実行手段 |
|---|---|---|
| LEVEL 0: AI不要 | 集計・フィルタリング・条件分岐・定型通知・API呼び出しの起動確認 | GitHub Actions / cron / 通常コード |
| LEVEL 1: 軽量AI | 短い要約・分類・定型文の微修正 | 可能な限り軽量モデル/API(将来的にClaude APIの軽量モデルを検討) |
| LEVEL 2: 高度AI | 創作・戦略判断・複雑な調査・アーキテクチャ設計 | Claude Code(セッション or Claude Code Remote Routine) |

判定に迷う場合は、**一段階下のLEVELとして扱い、通常コードでの実装を先に検討する**(「AIをたくさん動かすこと」ではなく「AIを使うべきところだけ使うこと」を優先する)。

## 4. Claude CodeとClaude APIの使い分け(将来方針)

現状、リポジトリ内にClaude API(Anthropic SDK)の直接利用は存在せず、すべてClaude Code Remoteに依存している(`AI_USAGE_AUDIT.md`セクション1参照)。今後、LEVEL 1〜2の処理を自動化パイプラインに組み込む際は:

- **Claude Code / Claude Code Remote**: 開発・保守・複雑な作業、および人間の承認フローと密接に結びついた戦略判断
- **Claude API(将来導入時)**: プロダクトの自動化処理内でAIが必要な場合(例: 投稿文生成をGitHub Actions内から直接Claude APIで呼び出す、等)。この場合、GitHub Actions側でAPIキーを管理し、Claude Code Remoteのセッションを起動する必要がなくなる

ただし、**単純処理まで無理にAPI化する必要はない**(セクション1のLEVEL 0は通常コードのままでよい)。

## 5. 頻度の目安

- **1日1回のAI判断を基本単位とする。** 常時監視・数分ごとの再確認は原則禁止する
- 定期的な「異常が無いか確認するだけ」のAI起動は禁止する。代わりに、通常コード側で異常検知を行い、**異常を検知した場合のみAIを起動する**Event Driven方式を優先する
- 既存Routineのうち2時間おき・4時間おきの頻度で動いているものは、`AI_AUTOMATION_ARCHITECTURE.md`の提案に沿って段階的に見直す

## 6. エラー時の無限ループ禁止

すべての自動化処理(Routine・GitHub Actions・スクリプト問わず)は、以下を実装すること:

- 最大リトライ回数の明記
- クールダウン期間
- 同一エラーの連続検知による自動停止(`threads-affiliate/automation_guard.py`のサーキットブレーカーパターンを他の自動化(BGM/moyasuka等)にも展開することを検討する)
- 停止時の通知手段

## 7. AI利用量の可視化(今後実装する場合の指針)

将来的にAI起動回数・トークン消費・コストを日次で記録する仕組みを追加する場合は、以下の項目を最低限含める:

```
date, workflow_or_routine_name, task, model,
execution_count, input_tokens, output_tokens,
estimated_cost, success, failure
```

目的は「どの自動化がAI利用量を消費しているのか分からない」という状態をなくすことであり、大規模な監視基盤を新設する必要はない(既存の「gitがすべての正、Markdown/JSONで管理」方針を踏襲し、JSON/JSONLファイルへの追記で十分)。

## 8. 安全装置(将来導入時の目安値)

利用状況が把握できるようになった段階で、以下のような自動停止の閾値導入を検討する(具体的なX/Yの値は、実際の利用データが蓄積されてから決定する。現時点でのAI起動回数実測値はセッションA約51回/日・セッションB約2回/日、コストは6週間で合計約$281):

- 1時間あたりのRoutine起動回数が異常値を超えたら自動停止
- 1日あたりのコストが一定額を超えたら通知(自動停止は事業影響を踏まえて要検討)
- 同一エラーが3回以上連続したら停止(既存の`automation_guard.py`と同じ基準)
- APIエラー・レスポンス異常の連続発生で停止

## 9. 判断基準(最重要)

```
通常コードで解決
        ↓
軽量AIで解決
        ↓
Claude API
        ↓
Claude Code
```

上に行くほど「本当に必要か?」を厳しく判断する。AIに仕事をさせること自体を目的にしない。

## 10. 本ポリシーの適用範囲

本ポリシーは2026-09-15以降の**新規**自動化に適用する。既存Routine 16件の見直しは`AI_AUTOMATION_ARCHITECTURE.md`の優先順位に沿って、オーナー確認の上で段階的に実施する。**このポリシー文書自体の制定は既存Routineへの変更を意味しない。**
