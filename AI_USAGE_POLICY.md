# AI Usage Policy — Claude Code / Claude API 利用ルール

**制定日**: 2026-09-15
**背景**: `AI_USAGE_AUDIT.md`の監査で、Claude Code Remoteの複数セッションが常時稼働し、想定外の利用量($1,340相当)を消費していたことが判明したため、今後の運用ルールを定める。

---

## 1. 基本原則

**「AIをたくさん動かすこと」を目的にしない。「AIを使うべきところだけ使うこと」を目的にする。**

新しい自動化・定期処理を追加する際は、必ず以下の順に検討する:

```
1. 通常コードで解決できないか?
        ↓ できない場合
2. 軽量AI(Claude API・単発呼び出し)で解決できないか?
        ↓ できない場合
3. Claude API(やや高度な処理)で解決できないか?
        ↓ できない場合
4. Claude Code Remote(持続セッション)が本当に必要か?
```

上に行くほど「本当に必要か」を厳しく判断する。**Claude Code Remoteの持続セッションを新しい定期処理のトリガーにすることを、デフォルトの選択肢にしない。**

---

## 2. Claude CodeとClaude APIの役割分担

| | Claude Code Remote(持続セッション) | Claude API(単発呼び出し) |
|---|---|---|
| 用途 | 開発・保守・大規模なコード変更・複雑な障害調査・システム監査・戦略判断・人間の承認が必要な意思決定 | プロダクトの自動化処理でAI判断・生成が必要な場合(記事本文生成、返信文生成など) |
| 起動方法 | 人間が明示的に開く、または週次のような**低頻度**のRoutineから起動する | GitHub Actions等の通常コードから、必要になった瞬間だけ呼ぶ |
| コンテキスト | 会話履歴が持続的に蓄積される(肥大化に注意) | 呼び出しごとに必要な情報だけを渡す(ステートレス) |
| コスト特性 | セッション単位で累積(気づきにくい) | 呼び出し単位で明確(見積もりやすい) |

**新しい自動化を作る際、「これは開発作業か、それとも定型的なプロダクト処理か」を最初に自問する。** 後者であれば、Claude Code Remoteの新しいRoutineを作る前に、GitHub Actions + Claude APIの組み合わせを先に検討する。

---

## 3. Routine(スケジュール実行)を新設する際のルール

新しいRoutineを作る前に、以下をすべて確認する:

- [ ] この処理の大部分(判定・集計・API呼び出し)は通常コードで書けるか? → 書けるなら、まずGitHub Actionsで実装する
- [ ] AIの判断が必要なのは、処理全体のうちどの部分か? その部分だけをClaude API呼び出しに切り出せるか?
- [ ] 頻度は本当に必要な頻度か?(「念のため頻繁に」を避ける。2時間毎・4時間毎のような高頻度Routineは、よほどの理由がない限り作らない)
- [ ] 同じ目的のRoutineが既に存在しないか?(`list_triggers`で確認する。「確実に起動」のような予備Routineを安易に追加しない。既存Routineが不安定なら、原因を直す方が先)
- [ ] このRoutineはどのセッションに紐付くか? 持続セッションのコンテキストが際限なく肥大化しないか?
- [ ] 失敗時の上限(最大リトライ回数・クールダウン・同一エラー検知での自動停止)を設計したか?

**特に「毎日」「毎時」「数分ごと」の頻度でClaude Code Remoteのセッションを起動するRoutineは、新設前に一度立ち止まって、GitHub Actionsで代替できないかを必ず検討する。**

---

## 4. 定期監査

- **週次経営レビュー(既存Routine)に、以下のチェックを追加する**:
  - `list_triggers`で全Routineの一覧・頻度を確認し、前回から増減がないか
  - `list_sessions`で稼働中セッションの`cost_usd`・`status_bucket`を確認し、`WORKING`のまま放置されているセッションがないか
  - 同一エラーで連続スキップしているRoutineがないか(3回以上なら是正または一時停止を検討)
- **月次で、`AI_USAGE_AUDIT.md`と同様の棚卸しを行い、差分を記録する**

---

## 5. 安全装置(運用ルール)

`AI_AUTOMATION_ARCHITECTURE.md`の閾値案を正式な運用ルールとする:

- 1時間あたりのセッション起動が10回を超えたら、該当Routine群を確認する
- 1日あたりのセッション起動が30回を超えるセッションは、次の週次レビューで棚卸し対象にする
- 同一Routineが同一原因で3回連続失敗/スキップした場合、Routineを無効化してオーナーに通知する(現状はこの仕組みがなく、BGM Shorts/長尺が16日間気づかれずにスキップされ続けた)
- セッションの累積コストが$100を超えたら、週次レビューでそのセッションの必要性を確認する
- `WORKING`のまま24時間以上応答がないセッションは、`interrupt_session`での状態確認、または`archive_session`でのアーカイブを検討する

---

## 6. やらないこと

- Claude Code Remoteを、単純な条件分岐・集計・API呼び出しのためだけに定期起動することは、原則として行わない
- 「念のため」の予備Routine・重複Routineを、根本原因を直さないまま追加することは行わない
- コンテキストを肥大化させたまま同一の持続セッションを無期限に使い続けることは行わない(定期的な棚卸しで判断する)
- 人間の確認なしに、既存のRoutine・セッションを大量に削除・停止することは行わない(このポリシー自体も、適用は段階的に行う)

---

## 7. このポリシーの見直し

- 初版は監査結果に基づく暫定ルール。実際にGitHub Actionsへの移行を進めた後、1ヶ月を目安に閾値・運用ルールを見直す
- 見直しの際は、`AI_USAGE_AUDIT.md`を再実行して効果を数値で確認する
